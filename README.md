# Getting Started with ULearn

Install local dependencies using:

### `npm install`

In the project directory, you can run:

### `npm start`

Open [http://localhost:5000](http://localhost:5000/) to view it in your browser.
